### Function: SetWis(spawn, value)

**Description:**
Set's the Spawn's wisdom to the value provided.

**Parameters:**
- `spawn` (Spawn) - Spawn reference object `spawn`.
- `value` (int32) - Integer value `value`.

**Returns:** None.

**Example:**

Example Required
